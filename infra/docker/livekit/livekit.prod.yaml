# ─────────────────────────────────────────────────────────────────────────────
# NOTICE: This file is a placeholder in the repository.
#
# The real LiveKit production config lives OUTSIDE the repository:
#   /opt/irno/secrets/livekit.prod.yaml   (chmod 0600, owned by root)
#
# This tracked copy contains NO real credentials and is NOT used at runtime.
# The production Docker Compose mounts /opt/irno/secrets/livekit.prod.yaml
# directly into the livekit container.
#
# To create the server config:
#   ./infra/scripts/first-deploy.sh   (generates it automatically)
#
# Or manually create /opt/irno/secrets/livekit.prod.yaml from the template:
#   infra/docker/livekit/livekit.prod.yaml.example
# ─────────────────────────────────────────────────────────────────────────────
