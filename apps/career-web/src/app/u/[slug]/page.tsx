// /u/[slug] — short URL alias for public profile pages (/public/[slug])
// Re-exports the same page component and metadata generator.

export { default, generateMetadata } from '../../public/[slug]/page'
